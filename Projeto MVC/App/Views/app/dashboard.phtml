<div class="container" style="color: white;">
    <?php


    $i = count($this->view->gastos) - 1;
    $data = $this->view->gastos[$i]['data'];
    $dados = [];

    function diferentes($data, $gastos)
    {
        $data = $gastos;
        return $data;
    }

    $valor = [];
    foreach ($this->view->gastos as $key => $gastos) {
        /*
        echo '<br>';
        echo 'Data do BD: '. $gastos['data'];
        echo '<br>';
        echo 'Valor do BD: '. $gastos['valor'];
        echo '<br>';
        echo 'Tipo do BD: '. $gastos['tipo'];
        echo '<br>';
        */
        if ($gastos['data'] != $data) {
            $data = diferentes($data, $gastos['data']);
            //echo $data;

        }
        $valor = [$data, $gastos['valor'], $gastos['tipo']];
        /*
        echo '<pre>';
        print_r($valor);
        echo '</pre>';
        echo '<hr>';*/
    }
    ?>
</div>